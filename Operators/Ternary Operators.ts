
let age : number = 7;

let ageTest : string = (age >= 18) ? "Eligible for voting" : "Don't come to voting booth";

console.log(ageTest);

let marks : number = 12;


let grade : string = (marks >= 90) ? "Distinction"  : 
                     (marks >= 75) ? "First Class" : 
                     (marks >= 50) ? "Second Class" : "Fail";


console.log(grade)
